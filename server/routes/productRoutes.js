const router=require('express').Router(),Product=require('../models/Product');router.get('/',async(req,res)=>res.json(await Product.find().sort({createdAt:-1})));router.get('/:id',async(req,res)=>{try{const p=await Product.findById(req.params.id);if(!p)return res.status(404).json({message:'Product not found'});res.json(p)}catch(e){res.status(404).json({message:'Product not found'})}});module.exports=router;
