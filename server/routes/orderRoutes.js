const router=require('express').Router(),Order=require('../models/Order'),auth=require('../middleware/auth');router.post('/',auth,async(req,res)=>{try{const items=req.body.items||[];if(!items.length)return res.status(400).json({message:'Cart is empty'});const total=items.reduce((s,i)=>s+i.price*i.quantity,0);const order=await Order.create({user:req.user.id,items,total});res.status(201).json(order)}catch(e){res.status(400).json({message:e.message})}});router.get('/my-orders',auth,async(req,res)=>res.json(await Order.find({user:req.user.id}).sort({createdAt:-1})));module.exports=router;
